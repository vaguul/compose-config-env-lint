# Changelog

## v0.1.2 - 2026-06-04

- Changed the package `bin` and export entrypoints to compiled `dist` files.
- Added `build`, `prepare`, and `prepack` scripts so GitHub/tag installs build the CLI correctly.
- Added package `types` and package file metadata for installable CLI usage.
- Documented installation from a GitHub release tag.

## v0.1.1 - 2026-06-01

- Added JSON output for CI usage.
- Documented Compose interpolation examples.

## v0.1.0 - 2026-06-01

- Initial CLI for detecting unescaped environment references inside `configs.*.content`.
