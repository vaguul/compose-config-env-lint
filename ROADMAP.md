# Roadmap

## Near Term

- Support more Compose interpolation examples in docs.
- Add fixture files for bad and good `configs.content` usage.
- Improve source locations for multiline block scalars.

## Later

- Optional JSON output for CI integrations.
- Optional rule for risky `${VAR:-default}` defaults in published snippets.
- Prebuilt npm package release if usage grows.
